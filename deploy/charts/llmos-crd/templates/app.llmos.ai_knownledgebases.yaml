---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    {}
  name: knownledgebases.app.llmos.ai
spec:
  group: app.llmos.ai
  names:
    kind: KnownledgeBase
    listKind: KnownledgeBaseList
    plural: knownledgebases
    shortNames:
    - kb
    - kbs
    singular: knownledgebase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: KnownledgeBase is a definition for the LLM KnownledgeBase
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              embeddingModel:
                description: EmbeddingModel is from model service including namespace,
                  such as defalt/text-embedding-3-small
                type: string
              files:
                items:
                  properties:
                    category:
                      description: Category is the application data name
                      type: string
                    uid:
                      type: string
                  required:
                  - category
                  - uid
                  type: object
                type: array
            required:
            - embeddingModel
            type: object
          status:
            properties:
              parsedFiles:
                items:
                  properties:
                    condition:
                      properties:
                        lastTransitionTime:
                          description: Last time the condition transitioned from one
                            status to another.
                          type: string
                        lastUpdateTime:
                          description: The last time this condition was updated.
                          type: string
                        message:
                          description: Human-readable message indicating details about
                            last transition
                          type: string
                        reason:
                          description: The reason for the condition's last transition.
                          type: string
                        status:
                          description: Status of the condition, one of True, False,
                            Unknown.
                          type: string
                        type:
                          description: Type of the condition.
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    file:
                      properties:
                        contentType:
                          type: string
                        etag:
                          type: string
                        isDir:
                          type: boolean
                        lastModified:
                          format: date-time
                          type: string
                        name:
                          type: string
                        path:
                          type: string
                        size:
                          format: int64
                          type: integer
                      required:
                      - contentType
                      - etag
                      - isDir
                      - lastModified
                      - name
                      - path
                      - size
                      type: object
                    importedTime:
                      format: date-time
                      type: string
                    uid:
                      type: string
                  required:
                  - condition
                  - file
                  - uid
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
